// import gsap

export class TooltipModel {
	// public showTooltip(tooltip, direction) {
	// 	gsap.to(tooltip, {});
	// }

	// public hideTooltip(tooltip, direction) {
	// 	gsap.to(tooltip, {});
	// }
}
