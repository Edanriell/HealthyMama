import { SwiperOptions } from "swiper";

export const ProductsSliderUniversal: SwiperOptions = {
	slidesPerView: "auto",
	loop: true,
	// centeredSlides: true,
	grabCursor: true,
	centeredSlides: true,
	spaceBetween: 18
	// centeredSlidesBounds: true
};

// Fix main-slider-universal padding !!!
